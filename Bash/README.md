# Bash Directory

This directory contains Bash tooling scripts.

## Files

- `Keyrings-Reader/`: a Bash utility that scans `/proc/keys` for `user` keys, attempts to dump them with `keyctl`, and saves successful results to local binary files.
