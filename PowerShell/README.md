# PowerShell Directory

This directory contains PowerShell tooling scripts.

## Files

- `Invoke-WindowsSearch.ps1`: a Windows Search index querying tool designed to search local or remote indexed content for sensitive keywords, optionally using a wordlist, remote sessions, filtering, and structured output formats.
