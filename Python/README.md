# Python Directory

This directory contains small Python utilities.

## Files

- `PDF-Encryptor/`: a command-line utility that reads a PDF, copies its pages into a new file, and applies password protection using the `pypdf` library.
