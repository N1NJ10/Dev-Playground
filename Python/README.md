# Python Directory

This directory contains small Python utilities.

## Files

- `PDF-Encryptor.py`: a command-line utility that opens a PDF, copies its pages into a new file, and applies password protection using the `pypdf` library.
